[project]
name = "inference-contracts"
version = "0.1.0"
description = "Shared contract definitions for inference-backbone"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
    "jsonschema>=4.0"
]

